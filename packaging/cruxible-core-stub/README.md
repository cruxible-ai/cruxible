# cruxible-core → cruxible

As of 0.2.0 the package is published as **[`cruxible`](https://pypi.org/project/cruxible/)**.
This stub exists so `pip install -U cruxible-core` keeps working for 0.1.x
installs — it depends on `cruxible` and contains nothing else. The import
name (`cruxible_core`) is unchanged.

Publish alongside the main package: `uv build` in this directory, then upload
with the same credentials/publisher as `cruxible`.
