[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cruxible-client"
version = "0.2.1"
description = "Client package for talking to a governed Cruxible daemon."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [
    {name = "Cruxible LLC"},
]
dependencies = [
    "httpx>=0.24",
    "pydantic>=2.0",
]

[project.urls]
Homepage = "https://github.com/cruxible-ai/cruxible"
Documentation = "https://github.com/cruxible-ai/cruxible/tree/main/docs"
Repository = "https://github.com/cruxible-ai/cruxible"

[tool.hatch.build.targets.wheel]
packages = ["src/cruxible_client"]
